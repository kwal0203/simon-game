name: Deploy to EC2

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: ${{ secrets.EC2_PORT }}
          script: |
            set -euo pipefail

            cd ~/apps/simon-game
            git fetch origin main
            git checkout main
            git pull --ff-only origin main

            docker compose --env-file .env.prod run --rm migrate
            docker compose --env-file .env.prod up --build -d redis api nginx

            # Wait for services to stabilize
            for i in {1..30}; do
              if curl -fsS http://localhost/health/db > /dev/null; then
                echo "Health check passed"
                exit 0
              fi
              echo "Health check failed ($i/30), retrying..."
              sleep 2
            done

            echo "Health check failed after retries"
            docker compose --env-file .env.prod ps
            docker compose --env-file .env.prod logs --tail=200 api nginx
            exit 1
